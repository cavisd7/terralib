version: 0.2
phases:
  pre_build:
    commands:
      - cd Stages/02_Infra
      - terraform init
      - terraform validate
  
  build:
    commands:
      - cd Stages/02_Infra
      - terraform apply --auto-approve